---
import Layout from "../layouts/Layout.astro";
---

<Layout title="404 - Pagina non trovata">
    <div
        class="error-container d-flex flex-column align-items-center justify-content-center text-center"
    >
        <div class="error-glass-card shadow-lg">
            <div class="error-code display-1 fw-bold mb-0">404</div>
            <div class="error-icon mb-4">
                <i
                    class="bi bi-calendar-x text-primary"
                    style="font-size: 5rem;"></i>
            </div>
            <h1 class="h2 mb-3 fw-bold">Ops! Evento non trovato</h1>
            <p class="text-muted mb-5 px-md-5">
                Sembra che la pagina che stai cercando sia stata cancellata,
                spostata o che non sia mai esistita nel nostro calendario.
            </p>
            <div class="d-flex gap-3 justify-content-center flex-wrap">
                <a
                    href="/"
                    class="btn btn-primary px-4 py-2 rounded-pill shadow-sm"
                >
                    <i class="bi bi-house-door me-2"></i> Torna alla Home
                </a>
                <a
                    href="/upcoming"
                    class="btn btn-outline-secondary px-4 py-2 rounded-pill"
                >
                    <i class="bi bi-calendar-event me-2"></i> Prossimi Eventi
                </a>
            </div>
        </div>
    </div>
</Layout>

<style>
    .error-container {
        min-height: 70vh;
        padding: 2rem 0;
    }

    .error-glass-card {
        background: var(--glass-bg);
        backdrop-filter: blur(15px);
        border: 1px solid var(--glass-border);
        border-radius: 30px;
        padding: 4rem 2rem;
        max-width: 600px;
        width: 100%;
        position: relative;
        overflow: hidden;
    }

    .error-glass-card::before {
        content: "";
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
            circle,
            rgba(99, 102, 241, 0.05) 0%,
            transparent 70%
        );
        z-index: -1;
    }

    .error-code {
        background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 8rem;
        line-height: 1;
        margin-bottom: -1rem;
        opacity: 0.9;
    }

    .error-icon i {
        display: inline-block;
        animation: float 4s ease-in-out infinite;
    }

    @keyframes float {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-20px);
        }
    }

    @media (max-width: 576px) {
        .error-code {
            font-size: 6rem;
        }
        .error-glass-card {
            padding: 3rem 1.5rem;
            border-radius: 20px;
        }
    }
</style>
