---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Registrazione Disabilitata - Shared Calendar">
    <div class="row justify-content-center mt-5">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm border-warning">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <i
                            class="bi bi-lock-fill text-warning"
                            style="font-size: 4rem;"></i>
                    </div>
                    <h2 class="card-title mb-3">Registrazione Disabilitata</h2>
                    <p class="text-muted mb-4">
                        La registrazione pubblica è stata disabilitata per
                        motivi di sicurezza.
                    </p>
                    <div class="alert alert-info" role="alert">
                        <i class="bi bi-info-circle-fill"></i>
                        <strong>Hai già un account?</strong><br />
                        Puoi effettuare il login utilizzando le tue credenziali.
                    </div>
                    <div class="alert alert-warning" role="alert">
                        <i class="bi bi-person-plus-fill"></i>
                        <strong>Nuovo utente?</strong><br />
                        Contatta l'amministratore del sistema per richiedere un account.
                    </div>
                    <div class="d-grid gap-2 mt-4">
                        <a href="/login" class="btn btn-primary btn-lg">
                            <i class="bi bi-box-arrow-in-right"></i> Vai al Login
                        </a>
                        <a href="/" class="btn btn-outline-secondary">
                            <i class="bi bi-house-door"></i> Torna alla Home
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        .card {
            border-width: 2px;
        }

        .bi-lock-fill {
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%,
            100% {
                opacity: 1;
            }
            50% {
                opacity: 0.6;
            }
        }
    </style>
</Layout>
